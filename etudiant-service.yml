server:
  port: 8083

spring:
  application:
    name: etudiant-service
  
  datasource:
    url: "jdbc:mysql://mysql-etudiant:3306/etudiant_db?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true"
    username: "root"
    password: "root"
    driver-class-name: "com.mysql.cj.jdbc.Driver"

  jpa:
    hibernate:
      ddl-auto: "update"
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: "org.hibernate.dialect.MySQL8Dialect"
